{
  "name": "get_ticket_info",
  "description": "El usuario podra obtener informacion del ticket o caso que solicite, para ello tienen que proporcionar el ID del cual vana a consultar.",
  "strict": true,
  "parameters": {
    "type": "object",
    "required": [
      "ticket_id"
    ],
    "properties": {
      "ticket_id": {
        "type": "string",
        "description": "ID del ticket o caso que se desea consultar"
      }
    },
    "additionalProperties": false
  }
}